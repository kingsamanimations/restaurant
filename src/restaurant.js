import pageLoad from './pageLoad';
import { createHeader, renderHomePage } from './restaurant';

// Element Data
const textData = {
    intro: `We serve food that's both culture, nutritious, and tasty! What would you like?`
}

const imageData = {}

// Restaurant.js
const createRestaurantHomePage = () => {
    const content = document.querySelector("#content");
    const pageContent = document.createElement('div');
    // pageContent.
}